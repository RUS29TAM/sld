import React from 'react';
import LoadingLane from "@/app/components/loading-lane/loading-lane";

const Page = () => {
    return (
        <div>
            <LoadingLane/>
            <h1 style={{textAlign: "center", color: '#fff'}}>Контакты</h1>
        </div>
    );
};

export default Page;
